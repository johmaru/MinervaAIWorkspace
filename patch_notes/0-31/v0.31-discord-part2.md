**UmansChat v0.31 Patch Notes (2/2)**

**Features**

- **TTFT model fallback** (`6df2496`): Auto-switch when first token is slow
- **Dynamic model selection UI** (`58759f9`): Dropdowns for fallback/thinking effort/search model, per-model reasoning levels
- **UmansAI provider hardcoded** (`05c6f90`): Removed OpenAI-compatible mode
- **sqlite-vec migration** (`24cd7f8`): JS cosine replaced with vec_distance_cosine for performance
- **Per-user Todo list** (`82e6a16`): Standalone Todo with vector embeddings + AI tool
- **Ctrl+Enter send mode** (`016dc32`): Toggle with localStorage persistence
- **Chat Markdown export** (`6a4f509`/`34bbfae`/`f56380e`): File export via CHAT_EXPORT_PATH, Docker host-path support
- **Configurable translate timeout** (`541658f`): TRANSLATE_TIMEOUT env var

**Bug Fixes**

- Kimi thinking tag handling in splitThinking (`dd5c07c`)
- sqlite-vec added to serverExternalPackages (`7c7f2ce`)
- sqlite-vec binaries added to output tracing (`64feb09`)
- Stale LLM_BASE_URL/LLM_MODELS refs cleaned up in docs (`25534cb`)

**Docs / Infra**

- Security audit findings + blocker fix plan (`2fb1341`)
- README and docs updated for Docker socket removal + Tor API change (`662901f`)
- work-completion-checklist migrated to alwaysApply rule (`278f4a7`)
- Completion-gate hook for work-completion-checklist enforcement (`26215a4`)

Full commit list: https://github.com/johmaru/UmansChat-Unofficial/compare/v0.3...develop